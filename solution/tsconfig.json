{
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "moduleResolution": "bundler",  // Keep this as it is - essential for Vite
    "jsx": "react-jsx",  // Or "react" if using newer React versions
    "strict": true,  // Recommended: Enforces stricter type checking
    "esModuleInterop": true, // Often helpful for compatibility
    "skipLibCheck": true, // Improves build performance
    "forceConsistentCasingInFileNames": true, // Enforces consistent casing
    "isolatedModules": true, // Recommended for build performance with Vite
    "noEmit": true, // Vite handles emitting JS; this prevents conflicts
    "incremental": true,  // Improves incremental build speeds
    "resolveJsonModule": true, // Enables importing JSON modules
    "types": ["vite/client"] // Necessary for Vite's type definitions
  },
  "include": ["src"], //  Sufficient in most cases; simpler than "src/**/*"
  "exclude": ["node_modules"]  //  "dist" is handled by Vite; no need to exclude
}
