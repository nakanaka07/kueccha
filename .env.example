# =============================================================================
# 佐渡で食えっちゃ - 環境変数設定カタログ
# =============================================================================
# このファイルは利用可能なすべての環境変数とその説明を含みます。
# 実際に使用する際はこのファイルをコピーして .env または .env.[mode] にリネームして使用してください。
# [mode] = development, test, production のいずれか

# ===== Google Maps API設定 =====
# 地図表示の核となる設定
VITE_GOOGLE_API_KEY=your_google_api_key_here           # Google Maps APIキー (string)
VITE_GOOGLE_MAPS_MAP_ID=your_map_id_here               # カスタムマップスタイル用のID (string)
VITE_GOOGLE_MAPS_VERSION=weekly                        # API読み込みバージョン (string: 'weekly'|'quarterly'|'beta')
VITE_GOOGLE_MAPS_LIBRARIES=places,marker               # 読み込むライブラリ (string: カンマ区切り)
VITE_GOOGLE_API_KEY_RESTRICTIONS=false                 # APIキー制限の有効化 (boolean as string: 'true'|'false')

# ===== データソース設定 =====
VITE_ENABLE_GOOGLE_SHEETS=false                        # GoogleSheetsデータソース使用 (boolean as string)
VITE_GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here    # スプレッドシートID (string)

# ===== ロギング設定 =====
VITE_LOG_LEVEL=info                                    # ログレベル (string: 'debug'|'info'|'warn'|'error')
VITE_DEBUG_MODE=false                                  # デバッグモード (boolean as string)
VITE_LOG_PERFORMANCE_THRESHOLD=150                     # パフォーマンスログ閾値(ms) (number as string)
VITE_DROP_CONSOLE=false                                # 本番ビルド時にconsole出力を削除 (boolean as string)

# ===== 機能フラグ設定 =====
# マーカー関連
VITE_ENABLE_MARKER_CLUSTERING=true                     # マーカークラスタリング (boolean as string)

# パフォーマンス最適化
VITE_OPTIMIZE_MAPS=false                               # 地図最適化モード (boolean as string)
VITE_MAP_LAZY_LOAD=false                               # 地図の遅延読み込み (boolean as string)
VITE_PRELOAD_POI_DATA=false                            # POIデータのプリロード (boolean as string)
VITE_ENABLE_COMPRESSION=false                          # 圧縮の有効化 (boolean as string)
VITE_MEASURE_MAP_PERFORMANCE=false                     # 地図パフォーマンス計測 (boolean as string)

# オフライン機能
VITE_ENABLE_OFFLINE_MODE=false                         # オフラインモード (boolean as string)
VITE_ENABLE_OFFLINE=false                              # オフライン機能 (boolean as string)

# ===== アプリケーション情報 =====
VITE_APP_NAME=佐渡で食えっちゃ                           # アプリケーション名 (string)
VITE_APP_SHORT_NAME=佐渡マップ                          # アプリケーション短縮名 (string)
VITE_APP_DESCRIPTION=佐渡島内の飲食店、駐車場、公共トイレの位置情報を網羅。  # アプリ説明 (string)
VITE_ENABLE_PWA=false                                  # PWA機能の有効化 (boolean as string)
VITE_PWA_CACHE_VERSION=1.0.0                           # PWAキャッシュバージョン (semver string)

# ===== 通信設定 =====
VITE_EMAILJS_SERVICE_ID=your_service_id_here           # EmailJS サービスID (string)
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here         # EmailJS テンプレートID (string)
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here           # EmailJS 公開キー (string)

# ===== デプロイメント設定 =====
VITE_BASE_PATH=/                                       # ベースパス (string)
VITE_STATIC_SITE=false                                 # 静的サイトモード (boolean as string)
VITE_STATIC_MAPS=false                                 # 静的地図使用 (boolean as string)

# =============================================================================
# 環境変数の使用方法
# =============================================================================
# 1. 各環境に適したファイルを作成:
#    - 開発環境: .env.development
#    - テスト環境: .env.test
#    - 本番環境: .env.production
#    - 共通設定: .env (すべての環境で利用)
#
# 2. 優先順位:
#    .env.{mode}.local > .env.{mode} > .env.local > .env
#
# 3. GitリポジトリにはAPIキーなどの機密情報を含む.envファイルをコミットしないこと
#    - .env.local と .env.*.local は .gitignore に追加すること
#    - .env.example はテンプレートとして共有する (機密情報なし)
#
# 4. GitHub Pages へのデプロイ時の環境変数設定:
#    - GitHub Secrets に機密情報を設定
#    - GitHub Actions ワークフローで環境変数を注入
# =============================================================================
