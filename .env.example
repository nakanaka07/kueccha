# =============================================================================
# 佐渡で食えっちゃ - 環境変数設定ファイル
# =============================================================================

# ==== 必須設定項目 ====
# -----------------------------------------------------------------------------

# Google Maps API Key（必須） - 地図表示と位置情報機能に必要
VITE_GOOGLE_API_KEY=your_maps_api_key_here

# Google Sheets ID（必須） - POI（スポット）データ管理用スプレッドシート
VITE_GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here

# ==== 基本アプリケーション設定 ====
# -----------------------------------------------------------------------------
# PWA設定
VITE_APP_NAME=佐渡で食えっちゃ         # アプリ正式名称（マニフェスト用）
VITE_APP_SHORT_NAME=食えっちゃ         # ホーム画面表示用の短縮名
VITE_APP_DESCRIPTION=佐渡島内の飲食店、駐車場、公共トイレの位置情報を網羅。快適な佐渡の旅をサポートします。

# ==== 拡張機能設定（任意） ====
# -----------------------------------------------------------------------------
# Google Maps カスタムスタイル用 Map ID（任意）
VITE_GOOGLE_MAPS_MAP_ID=your_map_id_here

# EmailJS関連設定（問い合わせフォーム機能を利用する場合に設定）
VITE_EMAILJS_SERVICE_ID=your_emailjs_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_emailjs_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key_here

# ==== デプロイメント設定 ====
# -----------------------------------------------------------------------------
# ベースパス - GitHub Pagesでのルートパス
# GitHub Actionsでは自動的に設定されるため手動設定は通常不要
BASE_PATH=/

# ビルドモード - 通常はGitHub Actionsで自動設定されるためコメントアウト
# NODE_ENV=production

# =============================================================================
# 環境変数のセットアップ手順
# =============================================================================
# 1. 開発環境: このファイルを「.env」という名前でコピー
# 2. コピーした.envファイルに実際の値を設定
# 3. .envファイルはGitにコミットしないこと（.gitignoreに記載済み）
#
# 本番環境（GitHub Pages）:
# - API KeyなどはGitHub Secretsに設定し、GitHub Actions経由で自動取得
# - GitHub Actionsのワークフローで環境変数が自動的に注入される
#
# 注意事項:
# - VITE_プレフィックスの環境変数のみクライアントコードで利用可能
# - すべての環境変数はビルド時に静的に埋め込まれる
# - 機密情報（APIキーなど）を公開リポジトリにコミットしないこと
# =============================================================================